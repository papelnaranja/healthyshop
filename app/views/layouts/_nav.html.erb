<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#"> HealthyShop</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">

      <li class="nav-item">
        <a class="nav-link" href="<%= new_user_session_path %>">Iniciar Sesion</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= new_user_registration_path %>">Registrarse</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="<%= destroy_user_session_path %>" data-method="DELETE">Salir</a>
      </li>
    </ul>
     <%# aca va el otro menu %>
      <% if user_signed_in? %>

         <ul class="navbar-nav">
          <li class="nav-item d-flex justify-content-start">
              <% if current_user.avatar != nil %>
                <div class="avatar"><%# image_tag current_user.sized(:avatar)  %></div> 
              <% end %>
              <%= current_user.name %>
              |
              <%= current_user.role %>
          </li>
          </ul>
        <% end %>

     <%# fin aca va el otro menu %>

       <% if user_signed_in? %>
    <% if current_user.role == "admin" %>
    <ul class="navbar-nav">
        <li class="nav-item"><%= link_to 'Backoffice' , backoffices_path %></li>
    </ul>
    <% end %>
    <% end %>
  </div>
</nav>

 